# 高校人才培养与就业大数据平台项目完成度评估与改进说明

## 1. 项目目标与五大模块概述

本项目面向“高校人才培养与就业大数据平台”课程设计/毕业设计场景，目标是围绕高校人才培养与就业管理过程，构建覆盖“数据生成/导入、算法分析、结果入库、后端服务、前端可视化展示”的完整系统。  
平台当前围绕以下五大模块组织：

1. 需求预测  
   基于历史就业薪资时间序列，利用 LSTM 模型预测未来一段时间的薪资趋势，用于辅助判断人才需求变化方向。

2. 招生匹配  
   基于协同过滤思想，对“专业”与“生源画像”之间的匹配关系进行建模，为招生宣传、投放与咨询提供方向性支持。

3. 培养方案优化  
   基于关联规则挖掘结果，识别“专业特征、技能特征、行业去向”之间的高关联模式，并进一步生成课程、技能、实践、结构优化建议。

4. 就业推荐  
   基于余弦相似度，对学生画像与岗位画像进行匹配，输出 Top-K 就业推荐结果及推荐原因，辅助就业指导。

5. 动态监测  
   基于后端聚合结果和可视化看板，对就业样本结构、行业流向、专业覆盖和薪资表现进行动态展示，为学校与主管部门提供监测入口。

---

## 2. 各模块算法与功能实现情况

### 2.1 需求预测模块

- 算法实现：`LSTM-all.py`
- 核心方法：Keras/TensorFlow LSTM 时间序列预测
- 输入数据：`fact_employment` 中按月份聚合的历史薪资数据
- 输出结果：
  - `ads_salary_forecast`
  - `ads_salary_forecast_eval`
  - `ads_salary_forecast_backtest`
- 后端接口：
  - `/api/salary-forecast`
  - `/api/salary-forecast-evaluation`
- 前端页面：
  - `frontend/src/pages/SalaryForecast.jsx`

当前已实现的功能包括：未来 12 个月薪资趋势预测、训练集/测试集切分、MAE/RMSE/MAPE 评估指标输出、真实值与预测值回测对比展示。

### 2.2 招生匹配模块

- 算法实现：`CF-all.py`
- 核心方法：ALS 协同过滤
- 输入数据：四表关联数据（学生、学业、就业、企业）
- 输出结果：
  - `ads_enrollment_matching`
  - `ads_enrollment_matching_eval`
- 后端接口：
  - `/api/enrollment-matching`
  - `/api/enrollment-matching-evaluation`
- 前端页面：
  - `frontend/src/pages/EnrollmentMatching.jsx`

当前实现的是“专业 - 生源画像”的匹配分析，而不是面向真实高考录取流程的精细化招生系统。评估部分采用近似离线评估口径，输出 Precision@K、Recall@K、HitRate@K。

### 2.3 培养方案优化模块

- 算法实现：
  - `FPgrowth-all.py`
  - `training_program_suggester.py`
- 核心方法：
  - FP-Growth 关联规则挖掘
  - 规则驱动的培养方案建议生成
- 输出结果：
  - `ads_major_matching_rules`
  - `ads_training_program_suggestions`
- 后端接口：
  - `/api/major-matching-rules`
  - `/api/training-program-optimization`
- 前端页面：
  - `frontend/src/pages/RuleAnalysis.jsx`
  - `frontend/src/pages/MajorOptimization.jsx`

当前不仅能展示 support、confidence、lift，还能基于规则强度、就业表现、平均薪资、行业/技能特征生成培养方案建议，覆盖课程模块、技能培养、实践教学和课程结构优化四类输出。

### 2.4 就业推荐模块

- 算法实现：`CF-all.py`
- 核心方法：余弦相似度匹配
- 输入数据：学生画像向量、岗位画像向量
- 输出结果：
  - `ads_job_recommendation`
  - `ads_job_recommendation_eval`
- 后端接口：
  - `/api/job-recommendation`
  - `/api/job-recommendation-evaluation`
- 前端页面：
  - `frontend/src/pages/JobRecommendation.jsx`

当前实现可以输出每个学生的 Top-3 推荐结果、相似度分数、推荐原因，并提供相似度统计类可信度指标。

### 2.5 动态监测模块

- 后端实现：`backend/app.py`
- 核心方式：基于数据库实时聚合查询
- 输入数据：
  - `dim_student`
  - `fact_academic`
  - `fact_employment`
- 输出接口：
  - `/api/employment-summary`
- 前端页面：
  - `frontend/src/pages/EmploymentMonitor.jsx`
  - `frontend/src/pages/Dashboard.jsx`

该模块不依赖单独机器学习模型，重点在于对就业样本结构、行业流向、专业覆盖与总体表现进行监测展示。

---

## 3. 当前已完成内容

### 3.1 数据与流程层

- 已实现一键全流程执行脚本 `run_full_pipeline.py`
- 已打通以下主链路：  
  数据生成/导入 -> 基础表建表 -> 特征加工 -> LSTM 预测 -> 协同过滤招生匹配 -> 关联规则挖掘 -> 余弦相似度就业推荐 -> 结果写入数据库 -> 后端接口读取 -> 前端页面展示
- 已增加阶段日志、依赖检查、结果表校验与失败中止机制

### 3.2 算法层

- 已实现 LSTM 需求预测及评估输出
- 已实现 ALS 招生匹配及近似评估输出
- 已实现 FP-Growth 规则挖掘及规则指标展示
- 已实现规则驱动的培养方案建议生成
- 已实现余弦相似度就业推荐及 Top-K 输出
- 已实现就业推荐可信度统计

### 3.3 后端层

- 已补齐主要分析接口
- 已新增模型评估接口与统一指标接口
- 已实现训练结果表、评估结果表与建议结果表的读取能力

### 3.4 前端层

- 已将五大模块做成可访问页面
- 已补充模型评估、算法说明、风险提示、数据更新时间、业务价值说明等答辩展示内容
- 已统一主要模块名称，增强平台化表达
- 已补齐“就业推荐”导航入口

---

## 4. 当前仍存在的不足

### 4.1 数据与实验口径方面

- 当前样本数据为平台生成数据，不是来源于真实高校业务系统的生产数据
- 招生匹配评估使用的是近似离线评估口径，尚未接入真实报名、报考、录取转化链路
- 就业推荐目前使用的是画像相似度匹配，尚未接入更细粒度的岗位要求文本、简历能力、招聘反馈等信息
- 培养方案优化中的部分指标仍属于“辅助判断口径”，不能等同于正式教务评价体系

### 4.2 工程与系统方面

- Spark JDBC 依赖在当前环境下仍可能回退到 CSV/SQLAlchemy 写入模式，说明运行环境依赖尚未完全统一
- 前端仍以课程设计展示为主，权限、审计、操作留痕、配置管理等企业级能力尚未建设
- 当前系统主要完成“分析与展示”，未延伸到真正的业务审批、通知、执行闭环

### 4.3 答辩与论文层面

- 目前实验设计与结论说明已经具备基础，但若用于论文写作，仍需补充更正式的实验设计章节、数据口径定义、变量说明和误差分析

---

## 5. 本次代码改造后解决的问题

本轮改造主要解决了以下几个方面的问题：

### 5.1 解决了“算法存在但未形成闭环”的问题

- 原有仓库中算法脚本、数据库结果表、后端接口、前端展示之间存在割裂
- 经过改造后，算法输出结果可以自动落库，并被后端接口与前端页面真实消费

### 5.2 解决了“一键运行不完整”的问题

- 原 `run_full_pipeline.py` 不能完整串联所有关键分析脚本
- 现已统一为一条主流程，并增加结果校验，真正实现“一键完成全流程”

### 5.3 解决了“前后端口径不一致”的问题

- 对模块命名、页面命名、接口命名进行了收口
- 补充了数据更新时间、业务价值说明、算法说明、风险提示
- 将若干旧的静态展示或歧义字段改为更符合实际口径的展示方式

### 5.4 解决了“只有结果，没有评估与解释”的问题

- LSTM 模块新增 MAE、RMSE、MAPE 与回测对比
- 招生匹配模块新增 Precision@K、Recall@K、HitRate@K
- 关联规则模块强化了 support、confidence、lift 的业务解释
- 就业推荐模块新增相似度得分、推荐原因、Top-K 展示与可信度统计

### 5.5 解决了“平台感不足”的问题

- 前端页面已不再只是图表堆叠
- 总览页能够串联五大模块的逻辑关系
- 各模块均可在答辩场景下独立说明“输入是什么、算法做了什么、输出有什么价值”

---

## 6. 系统整体流程图（文字描述）

系统整体流程可概括为以下链路：

1. 使用样本数据工厂生成学生、学业、企业、就业等基础数据
2. 执行数据库建表脚本，初始化维度表与事实表
3. 将基础样本数据导入 MySQL
4. 通过 Spark 脚本完成特征加工与基础 ADS 聚合
5. 使用 LSTM 对历史薪资序列进行训练与预测，输出需求预测结果与评估结果
6. 使用 ALS 协同过滤计算“专业 - 生源画像”匹配关系，并输出近似评估结果
7. 使用 FP-Growth 挖掘专业、技能、行业之间的关联规则
8. 在关联规则结果基础上生成培养方案优化建议
9. 使用余弦相似度计算学生画像与岗位画像的匹配度，输出 Top-K 就业推荐结果与可信度统计
10. 将上述分析结果统一写入数据库结果表
11. 后端 Flask 接口从数据库读取结果，向前端提供统一数据服务
12. 前端页面按模块展示监测、预测、匹配、规则、优化、推荐与专报内容

---

## 7. 后续可扩展方向

### 7.1 数据侧扩展

- 接入真实高校招生、教务、就业等业务系统数据
- 引入岗位招聘文本、技能关键词、企业反馈等更丰富特征
- 增加毕业去向跟踪、升学去向、城市流动等辅助维度

### 7.2 算法侧扩展

- 在需求预测中引入多变量时间序列特征，而不仅限于薪资序列
- 在招生匹配中引入更真实的用户-项目反馈矩阵和交叉验证
- 在就业推荐中融合文本向量、知识图谱或大模型语义匹配能力
- 在培养方案优化中引入更系统的课程图谱与能力图谱

### 7.3 系统侧扩展

- 增加权限分级、数据审计、结果导出、模板化报告等功能
- 增加“专业负责人视角”“学院视角”“学校管理层视角”的分层展示
- 增加从建议到执行的业务闭环，例如方案确认、整改跟踪、效果复盘

### 7.4 答辩与论文侧扩展

- 补充实验设计、变量定义、评价标准、误差分析章节
- 增加典型案例分析和模块间联动分析
- 增加系统性能、可扩展性与局限性分析

---

## 8. 结论

综合当前仓库实现情况，可以认为本项目已经完成了一个较完整的“高校人才培养与就业大数据平台”原型系统建设。  
其主要特点是：

- 五大模块均已有真实代码实现和页面承载
- 关键算法已能产出数据库结果
- 后端接口与前端展示已形成闭环
- 系统已具备课程答辩、课程设计展示和毕业设计原型演示的基础条件

同时也需要客观说明：

- 当前平台更适合作为“教学型、原型型、答辩型”系统，而非可直接上线的真实业务平台
- 若继续向论文或真实项目演进，仍需在数据真实性、实验严谨性、系统工程化方面持续完善

总体而言，项目已经从“算法脚本集合”升级为“具备全流程、可展示、可解释、可评估”的综合性平台原型，能够较好支撑课程答辩和项目汇报。
