# FRONTEND_PAGE_CHECK_REPORT

本报告由 `python scripts/check_frontend_pages.py` 生成，采用静态扫描方式发现前端页面展示风险。

- 扫描目录：`frontend\src`
- 发现项：180

| 规则 | 文件 | 行号 | 风险说明 | 建议修复 | 命中代码 |
|---|---:|---:|---|---|---|
| 图表数值强制补 0 | `frontend\src\components\GovernmentDrillBoard.jsx` | 219 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `{ title: '就业率', value: Number(schoolOverview.employment_rate \|\| 0), suffix: '%', style: statValueCyan },` |
| 图表数值强制补 0 | `frontend\src\components\GovernmentDrillBoard.jsx` | 226 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `{ title: '就业率', value: Number(majorOverview.employment_rate \|\| 0), suffix: '%', style: statValueCyan },` |
| 图表数值强制补 0 | `frontend\src\components\GovernmentDrillBoard.jsx` | 238 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `{ title: '就业率', dataIndex: 'employment_rate', render: (value) => `${Number(value \|\| 0).toFixed(1)}%` },` |
| 图表数值强制补 0 | `frontend\src\components\GovernmentDrillBoard.jsx` | 240 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `{ title: '先导产业占比', dataIndex: 'strategic_ratio', render: (value) => `${Number(value \|\| 0).toFixed(1)}%` },` |
| 图表数值强制补 0 | `frontend\src\components\GovernmentDrillBoard.jsx` | 315 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `{Number(majorDetail?.enrollment_hint?.avg_match_score \|\| 0).toFixed(3)}` |
| 图表数值强制补 0 | `frontend\src\components\GovernmentDrillBoard.jsx` | 320 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `<div style={metaValueStyle}>{formatNumber(majorDetail?.enrollment_hint?.total_sample_size \|\| 0)}</div>` |
| 图表数值强制补 0 | `frontend\src\components\GovernmentDrillBoard.jsx` | 324 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `<div style={metaValueStyle}>{Number(majorOverview.strategic_ratio \|\| 0).toFixed(1)}%</div>` |
| 图表数值强制补 0 | `frontend\src\components\HeroInfoStrip.jsx` | 14 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `<div className="hero-info-strip__value">{schoolCount \|\| 0} 所</div>` |
| 图表数值强制补 0 | `frontend\src\components\SchoolBenchmarkBoard.jsx` | 51 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `rate ? `就业率：${Number(rate.value \|\| 0).toFixed(1)}%` : '',` |
| 图表数值强制补 0 | `frontend\src\components\SchoolBenchmarkBoard.jsx` | 52 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `strategic ? `高质量就业占比：${Number(strategic.value \|\| 0).toFixed(1)}%` : '',` |
| 图表数值强制补 0 | `frontend\src\components\SchoolBenchmarkBoard.jsx` | 95 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `data: rows.map((item) => Number(item.avg_salary \|\| 0)),` |
| 图表数值强制补 0 | `frontend\src\components\SchoolBenchmarkBoard.jsx` | 105 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `data: rows.map((item) => Number(item.employment_rate \|\| 0)),` |
| 图表数值强制补 0 | `frontend\src\components\SchoolBenchmarkBoard.jsx` | 116 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `data: rows.map((item) => Number(item.strategic_ratio \|\| 0)),` |
| 图表数值强制补 0 | `frontend\src\components\SchoolBenchmarkBoard.jsx` | 137 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `rate ? `就业率：${Number(rate.value \|\| 0).toFixed(1)}%` : '',` |
| 图表数值强制补 0 | `frontend\src\components\SchoolBenchmarkBoard.jsx` | 180 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `data: rows.map((item) => Number(item.avg_salary \|\| 0)),` |
| 图表数值强制补 0 | `frontend\src\components\SchoolBenchmarkBoard.jsx` | 190 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `data: rows.map((item) => Number(item.employment_rate \|\| 0)),` |
| 图表数值强制补 0 | `frontend\src\components\SchoolBenchmarkBoard.jsx` | 265 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `return [...baseRows].sort((a, b) => Number(b.avg_salary \|\| 0) - Number(a.avg_salary \|\| 0))` |
| 图表数值强制补 0 | `frontend\src\components\SchoolBenchmarkBoard.jsx` | 272 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `return [...baseRows].sort((a, b) => Number(b.avg_salary \|\| 0) - Number(a.avg_salary \|\| 0))` |
| 图表数值强制补 0 | `frontend\src\components\SchoolBenchmarkBoard.jsx` | 287 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `avgEmploymentRate: visibleOverview.reduce((sum, item) => sum + Number(item.employment_rate \|\| 0), 0) / total,` |
| 图表数值强制补 0 | `frontend\src\components\SchoolBenchmarkBoard.jsx` | 288 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `avgSalary: visibleOverview.reduce((sum, item) => sum + Number(item.avg_salary \|\| 0), 0) / total,` |
| 图表数值强制补 0 | `frontend\src\components\SchoolBenchmarkBoard.jsx` | 289 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `totalStudents: visibleOverview.reduce((sum, item) => sum + Number(item.student_count \|\| 0), 0),` |
| 图表数值强制补 0 | `frontend\src\components\SchoolBenchmarkBoard.jsx` | 307 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `{ title: '就业率', dataIndex: 'employment_rate', render: (value) => `${Number(value \|\| 0).toFixed(1)}%` },` |
| 图表数值强制补 0 | `frontend\src\components\SchoolBenchmarkBoard.jsx` | 312 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `render: (value) => `${Number(value \|\| 0).toFixed(1)}%`,` |
| 图表数值强制补 0 | `frontend\src\components\SchoolBenchmarkBoard.jsx` | 321 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `{ title: '就业率', dataIndex: 'employment_rate', render: (value) => `${Number(value \|\| 0).toFixed(1)}%` },` |
| 图表数值强制补 0 | `frontend\src\components\SchoolBenchmarkBoard.jsx` | 326 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `render: (value) => `${Number(value \|\| 0).toFixed(1)}%`,` |
| 图表数值强制补 0 | `frontend\src\components\SchoolMapExplorer.jsx` | 30 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `const maxEmp = Math.max(...points.map((item) => Number(item.total_emp \|\| 0)), 1)` |
| 图表数值强制补 0 | `frontend\src\components\SchoolMapExplorer.jsx` | 106 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `const emp = Number(params?.data?.detail?.total_emp \|\| 0)` |
| 图表数值强制补 0 | `frontend\src\components\SchoolMapExplorer.jsx` | 487 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `{renderStatCard('先导产业占比', `${Number(selectedDetail.strategic_ratio \|\| 0).toFixed(1)}%`, '#d97706')}` |
| 笼统失败提示 | `frontend\src\hooks\usePlatformData.js` | 168 | 接口失败时用户难以定位具体模块。 | 提示中列出失败模块名，并在 console.warn 输出原因。 | `setError(`以下模块数据暂未返回：${failedKeys.join('、')}。当前页面已使用可用数据继续渲染。`)` |
| 图表数值强制补 0 | `frontend\src\pages\AlgorithmChain.jsx` | 12 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `const rows = [...algorithmChainLogData].sort((a, b) => Number(a.stage_order \|\| 0) - Number(b.stage_order \|\| 0))` |
| 图表数值强制补 0 | `frontend\src\pages\AlgorithmChain.jsx` | 32 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `<div>耗时：{Number(item.cost_seconds \|\| 0).toFixed(2)} 秒</div>` |
| 图表数值强制补 0 | `frontend\src\pages\AlgorithmChain.jsx` | 49 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `{ title: '耗时秒数', dataIndex: 'cost_seconds', render: (value) => Number(value \|\| 0).toFixed(2) },` |
| 图表数值强制补 0 | `frontend\src\pages\Dashboard.jsx` | 80 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `const index = params?.[0]?.dataIndex ?? 0` |
| 图表数值强制补 0 | `frontend\src\pages\Dashboard.jsx` | 85 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | ``支持度：${(Number(matched.support \|\| 0) * 100).toFixed(1)}%`,` |
| 图表数值强制补 0 | `frontend\src\pages\Dashboard.jsx` | 86 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | ``置信度：${(Number(matched.confidence \|\| 0) * 100).toFixed(1)}%`,` |
| 图表数值强制补 0 | `frontend\src\pages\Dashboard.jsx` | 87 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | ``提升度：${Number(matched.lift \|\| 0).toFixed(2)}`,` |
| 图表数值强制补 0 | `frontend\src\pages\Dashboard.jsx` | 104 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `data: rules.map((item) => Number(item.lift \|\| 0)),` |
| 空图表渲染风险 | `frontend\src\pages\Dashboard.jsx` | 372 | 图表可能在空数据时仍渲染坐标轴。 | 渲染前判断 series/data 是否为空，空时显示 Empty。 | `<ReactECharts option={buildDisciplineOption(disciplineData)} style={{ height: 400 }} />` |
| 图表数值强制补 0 | `frontend\src\pages\Dashboard.jsx` | 385 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `{ title: '支持度', dataIndex: 'support', render: (value) => `${(Number(value \|\| 0) * 100).toFixed(1)}%` },` |
| 图表数值强制补 0 | `frontend\src\pages\Dashboard.jsx` | 386 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `{ title: '置信度', dataIndex: 'confidence', render: (value) => `${(Number(value \|\| 0) * 100).toFixed(1)}%` },` |
| 图表数值强制补 0 | `frontend\src\pages\Dashboard.jsx` | 387 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `{ title: '提升度', dataIndex: 'lift', render: (value) => Number(value \|\| 0).toFixed(2) },` |
| 图表数值强制补 0 | `frontend\src\pages\DemandForecast.jsx` | 61 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `highMajorCount: new Set(rows.filter((item) => item.demand_level === '高需求' \|\| Number(item.predicted_demand_count \|\| 0) >= 80).map((item) => item.major_name)).size,` |
| 图表数值强制补 0 | `frontend\src\pages\DemandForecast.jsx` | 62 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `topJobCategory: rows.reduce((best, item) => Number(item.predicted_demand_count \|\| 0) > Number(best.predicted_demand_count \|\| 0) ? item : best, {}),` |
| 图表数值强制补 0 | `frontend\src\pages\DemandForecast.jsx` | 106 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `const mape = Number(metrics.find((item) => item.metric_name === 'MAPE')?.metric_value \|\| 0)` |
| 图表数值强制补 0 | `frontend\src\pages\DemandForecast.jsx` | 107 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `const topSkills = [...jobSkillsHeatmapData].sort((a, b) => Number(b.skill_count \|\| 0) - Number(a.skill_count \|\| 0)).slice(0, 8)` |
| 图表数值强制补 0 | `frontend\src\pages\DemandForecast.jsx` | 154 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `{ title: '增长率', dataIndex: 'demand_growth_rate', render: (value) => `${(Number(value \|\| 0) * 100).toFixed(1)}%` },` |
| 图表数值强制补 0 | `frontend\src\pages\EmploymentMonitor.jsx` | 55 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `emp_count: Number(item?.emp_count \|\| 0),` |
| 图表数值强制补 0 | `frontend\src\pages\EmploymentMonitor.jsx` | 56 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `avg_salary: Number(item?.avg_salary \|\| 0),` |
| 图表数值强制补 0 | `frontend\src\pages\EnrollmentMatching.jsx` | 37 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `return Number(item.matching_score ?? item.match_score ?? 0)` |
| 图表数值强制补 0 | `frontend\src\pages\EnrollmentMatching.jsx` | 46 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `return Number(item.sample_size ?? item.sample_count ?? item.profile_count ?? 0)` |
| 图表数值强制补 0 | `frontend\src\pages\EnrollmentMatching.jsx` | 164 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `const avg = (getter) => selectedRows.reduce((sum, item) => sum + Number(getter(item) \|\| 0), 0) / selectedRows.length` |
| 图表数值强制补 0 | `frontend\src\pages\EnrollmentMatching.jsx` | 166 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `const matchBase = getComparableScore(base) \|\| avg(getComparableScore) \|\| 0.68` |
| 图表数值强制补 0 | `frontend\src\pages\EnrollmentMatching.jsx` | 197 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `avg_salary: Number(base.avg_salary \|\| 0),` |
| 图表数值强制补 0 | `frontend\src\pages\EnrollmentMatching.jsx` | 198 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `employment_rate: Number(base.employment_rate \|\| 0),` |
| 图表数值强制补 0 | `frontend\src\pages\EnrollmentMatching.jsx` | 199 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `demand_growth_rate: Number(base.demand_growth_rate \|\| 0),` |
| 图表数值强制补 0 | `frontend\src\pages\EnrollmentMatching.jsx` | 225 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | ``综合画像得分：${Number(item.profile_score \|\| 0).toFixed(1)}`,` |
| 图表数值强制补 0 | `frontend\src\pages\EnrollmentMatching.jsx` | 226 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | ``样本量：${Number(item.profile_count \|\| 0).toFixed(0)}`,` |
| 图表数值强制补 0 | `frontend\src\pages\EnrollmentMatching.jsx` | 227 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | ``匹配分：${Number(item.match_score \|\| 0).toFixed(2)}`,` |
| 图表数值强制补 0 | `frontend\src\pages\EnrollmentMatching.jsx` | 333 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `value={Number(evalMap['Precision@K']?.metric_value \|\| 0)}` |
| 图表数值强制补 0 | `frontend\src\pages\JobRecommendation.jsx` | 122 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `{ title: '相似度', dataIndex: 'matching_score', render: (value) => Number(value \|\| 0).toFixed(3) },` |
| 图表数值强制补 0 | `frontend\src\pages\JobRecommendation.jsx` | 151 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `<Col xs={24} md={12}><Statistic title="Top1 平均相似度" value={evalMap.AvgTop1Similarity?.metric_value \|\| 0} precision={3} styles={{ title: statTitleStyle, content: statValuePrimary }} ` |
| 图表数值强制补 0 | `frontend\src\pages\JobRecommendation.jsx` | 152 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `<Col xs={24} md={12}><Statistic title="高置信推荐占比" value={(evalMap.HighConfidenceRatio?.metric_value \|\| 0) * 100} precision={2} suffix="%" styles={{ title: statTitleStyle, content: st` |
| 字段名一致性风险 | `frontend\src\pages\MajorOptimization.jsx` | 66 | camelCase 与后端 snake_case 混用可能导致读取为空。 | 数据适配层统一兼容 snake_case/camelCase。 | `const matchScore = percentLike(item.avg_match_score ?? item.match_score)` |
| 字段名一致性风险 | `frontend\src\pages\MajorOptimization.jsx` | 68 | camelCase 与后端 snake_case 混用可能导致读取为空。 | 数据适配层统一兼容 snake_case/camelCase。 | `return policyHeat * 0.3 + demandGrowth * 0.25 + skillGap * 0.2 + matchScore * 0.15 + evidenceScore * 0.1` |
| 图表数值强制补 0 | `frontend\src\pages\MajorOptimization.jsx` | 111 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | ``优先级：${Number(item.priority_score \|\| 0).toFixed(1)}`,` |
| 图表数值强制补 0 | `frontend\src\pages\MajorOptimization.jsx` | 112 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | ``平均薪资：${formatNumber(item.avg_salary \|\| 0, 0)} 元`,` |
| 图表数值强制补 0 | `frontend\src\pages\MajorOptimization.jsx` | 113 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | ``样本量：${Number(item.sample_size \|\| 0).toFixed(0)}`,` |
| 图表数值强制补 0 | `frontend\src\pages\MajorOptimization.jsx` | 115 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | ``技能缺口：${Number(item.skill_gap_score \|\| 0).toFixed(2)}`,` |
| 图表数值强制补 0 | `frontend\src\pages\MajorOptimization.jsx` | 116 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | ``政策热度：${Number(item.policy_heat \|\| 0).toFixed(1)}`,` |
| 图表数值强制补 0 | `frontend\src\pages\MajorOptimization.jsx` | 461 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `<Tag color="cyan">优先级 {Number(item.priority_score \|\| 0).toFixed(1)}</Tag>` |
| 图表数值强制补 0 | `frontend\src\pages\MajorOptimization.jsx` | 489 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `{ title: '优先级', dataIndex: 'priority_score', width: 100, render: (value) => Number(value \|\| 0).toFixed(1) },` |
| 图表数值强制补 0 | `frontend\src\pages\MajorOptimization.jsx` | 494 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `render: (value) => `${Number(value \|\| 0).toFixed(1)}%`,` |
| 图表数值强制补 0 | `frontend\src\pages\MajorOptimization.jsx` | 496 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `{ title: '平均薪资', dataIndex: 'avg_salary', width: 120, render: (value) => `${formatNumber(value \|\| 0, 0)} 元` },` |
| 图表数值强制补 0 | `frontend\src\pages\MajorOptimization.jsx` | 501 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | ``S ${Number(record.top_rule_support \|\| 0).toFixed(2)} / C ${Number(record.top_rule_confidence \|\| 0).toFixed(2)} / L ${Number(record.top_rule_lift \|\| 0).toFixed(2)}`,` |
| 图表数值强制补 0 | `frontend\src\pages\PortalHome.jsx` | 119 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `{ title: '样本覆盖高校数', value: overview?.schoolCount \|\| 0, style: statValuePrimary },` |
| 图表数值强制补 0 | `frontend\src\pages\PortalHome.jsx` | 120 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `{ title: '公开样本平均薪资', value: overview?.avgSalary \|\| 0, suffix: '元', style: statValueBlue },` |
| 图表数值强制补 0 | `frontend\src\pages\PublicWorkspace.jsx` | 29 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `const values = data.map((item) => Number(item.avg_salary \|\| 0))` |
| 图表数值强制补 0 | `frontend\src\pages\PublicWorkspace.jsx` | 39 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `const index = params?.[0]?.dataIndex ?? 0` |
| 图表数值强制补 0 | `frontend\src\pages\PublicWorkspace.jsx` | 51 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `formatter: (value) => `${Math.round(Number(value \|\| 0) / 1000)}k`,` |
| 图表数值强制补 0 | `frontend\src\pages\PublicWorkspace.jsx` | 64 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `data: data.map((item) => Number(item.avg_salary \|\| 0)),` |
| 图表数值强制补 0 | `frontend\src\pages\PublicWorkspace.jsx` | 77 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `const salaryValues = data.map((item) => Number(item.avg_salary \|\| 0))` |
| 图表数值强制补 0 | `frontend\src\pages\PublicWorkspace.jsx` | 78 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `const sampleValues = data.map((item) => Number(item.sample_count \|\| 0))` |
| 图表数值强制补 0 | `frontend\src\pages\PublicWorkspace.jsx` | 88 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `const index = params?.[0]?.dataIndex ?? 0` |
| 图表数值强制补 0 | `frontend\src\pages\PublicWorkspace.jsx` | 95 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | ``先导产业占比：${Number(item.strategic_ratio \|\| 0).toFixed(1)}%`,` |
| 图表数值强制补 0 | `frontend\src\pages\RuleAnalysis.jsx` | 22 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `const index = params?.[0]?.dataIndex ?? 0` |
| 图表数值强制补 0 | `frontend\src\pages\RuleAnalysis.jsx` | 28 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | ``支持度：${(Number(item.support \|\| 0) * 100).toFixed(1)}%`,` |
| 图表数值强制补 0 | `frontend\src\pages\RuleAnalysis.jsx` | 29 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | ``置信度：${(Number(item.confidence \|\| 0) * 100).toFixed(1)}%`,` |
| 图表数值强制补 0 | `frontend\src\pages\RuleAnalysis.jsx` | 30 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | ``提升度：${Number(item.lift \|\| 0).toFixed(2)}`,` |
| 图表数值强制补 0 | `frontend\src\pages\RuleAnalysis.jsx` | 31 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | ``证据分：${Number(item.evidence_score \|\| 0).toFixed(2)}`,` |
| 图表数值强制补 0 | `frontend\src\pages\RuleAnalysis.jsx` | 49 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `data: data.map((item) => Number(Number(item.evidence_score \|\| 0).toFixed(2))),` |
| 图表数值强制补 0 | `frontend\src\pages\RuleAnalysis.jsx` | 56 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `data: data.map((item) => Number((Number(item.support \|\| 0) * 100).toFixed(1))),` |
| 图表数值强制补 0 | `frontend\src\pages\RuleAnalysis.jsx` | 70 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `return `${cleanRuleLabel(params.data.source)} → ${cleanRuleLabel(params.data.target)}<br/>提升度：${Number(params.data.value \|\| 0).toFixed(2)}`` |
| 图表数值强制补 0 | `frontend\src\pages\RuleAnalysis.jsx` | 131 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `{ title: '支持度', dataIndex: 'support', render: (value) => `${(Number(value \|\| 0) * 100).toFixed(1)}%` },` |
| 图表数值强制补 0 | `frontend\src\pages\RuleAnalysis.jsx` | 132 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `{ title: '置信度', dataIndex: 'confidence', render: (value) => `${(Number(value \|\| 0) * 100).toFixed(1)}%` },` |
| 图表数值强制补 0 | `frontend\src\pages\RuleAnalysis.jsx` | 133 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `{ title: '提升度', dataIndex: 'lift', render: (value) => Number(value \|\| 0).toFixed(2) },` |
| 空图表渲染风险 | `frontend\src\pages\RuleAnalysis.jsx` | 152 | 图表可能在空数据时仍渲染坐标轴。 | 渲染前判断 series/data 是否为空，空时显示 Empty。 | `<ReactECharts option={buildRulesGraphOption(graphData)} style={{ height: 420 }} />` |
| 图表数值强制补 0 | `frontend\src\utils\dataAdapter.js` | 51 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `return Number(num \|\| 0).toLocaleString('zh-CN', {` |
| 图表数值强制补 0 | `frontend\src\utils\dataAdapter.js` | 66 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `avg_salary: Number(item.avg_salary \|\| 0),` |
| 图表数值强制补 0 | `frontend\src\utils\dataAdapter.js` | 67 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `emp_count: Number(item.emp_count \|\| 0),` |
| 图表数值强制补 0 | `frontend\src\utils\dataAdapter.js` | 68 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `high_tech_ratio: Number(item.high_tech_ratio \|\| 0),` |
| 图表数值强制补 0 | `frontend\src\utils\dataAdapter.js` | 99 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `support: Number(item.support \|\| 0),` |
| 图表数值强制补 0 | `frontend\src\utils\dataAdapter.js` | 100 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `confidence: Number(item.confidence \|\| 0),` |
| 图表数值强制补 0 | `frontend\src\utils\dataAdapter.js` | 101 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `lift: Number(item.lift \|\| 0),` |
| 图表数值强制补 0 | `frontend\src\utils\dataAdapter.js` | 102 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `evidence_score: Number(item.evidence_score \|\| 0),` |
| 图表数值强制补 0 | `frontend\src\utils\dataAdapter.js` | 144 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `current.monthMap.set(item.forecast_month, (current.monthMap.get(item.forecast_month) \|\| 0) + value)` |
| 图表数值强制补 0 | `frontend\src\utils\dataAdapter.js` | 145 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `current.jobMap.set(item.job_category_name, (current.jobMap.get(item.job_category_name) \|\| 0) + value)` |
| 图表数值强制补 0 | `frontend\src\utils\dataAdapter.js` | 155 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `fallback.monthMap.set(item.forecast_month, (fallback.monthMap.get(item.forecast_month) \|\| 0) + value)` |
| 图表数值强制补 0 | `frontend\src\utils\dataAdapter.js` | 374 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `counts.set(label, (counts.get(label) \|\| 0) + 1)` |
| 图表数值强制补 0 | `frontend\src\utils\dataAdapter.js` | 391 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `current.total += Number(item.predicted_demand_count \|\| 0)` |
| 图表数值强制补 0 | `frontend\src\utils\dataAdapter.js` | 396 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `const weightedEmpCount = scopedEmployment.reduce((sum, item) => sum + Number(item.emp_count \|\| 0), 0)` |
| 图表数值强制补 0 | `frontend\src\utils\dataAdapter.js` | 398 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `? scopedEmployment.reduce((sum, item) => sum + Number(item.avg_salary \|\| 0) * Number(item.emp_count \|\| 0), 0) / weightedEmpCount` |
| 图表数值强制补 0 | `frontend\src\utils\dataAdapter.js` | 510 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `metric_value: Number(item.metric_value \|\| 0),` |
| 图表数值强制补 0 | `frontend\src\utils\dataAdapter.js` | 513 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `sample_size: Number(item.sample_size \|\| 0),` |
| 图表数值强制补 0 | `frontend\src\utils\dataAdapter.js` | 514 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `train_window_size: Number(item.train_window_size \|\| 0),` |
| 图表数值强制补 0 | `frontend\src\utils\dataAdapter.js` | 515 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `test_window_size: Number(item.test_window_size \|\| 0),` |
| 图表数值强制补 0 | `frontend\src\utils\dataAdapter.js` | 516 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `k_value: Number(item.k_value \|\| 0),` |
| 图表数值强制补 0 | `frontend\src\utils\dataAdapter.js` | 517 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `evaluated_profiles: Number(item.evaluated_profiles \|\| 0),` |
| 图表数值强制补 0 | `frontend\src\utils\dataAdapter.js` | 532 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `actual_demand_count: Number(item.actual_demand_count ?? item.actual_salary ?? 0),` |
| 图表数值强制补 0 | `frontend\src\utils\dataAdapter.js` | 533 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `predicted_demand_count: Number(item.predicted_demand_count ?? item.predicted_salary ?? 0),` |
| 图表数值强制补 0 | `frontend\src\utils\dataAdapter.js` | 534 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `abs_error: Number(item.abs_error \|\| 0),` |
| 图表数值强制补 0 | `frontend\src\utils\dataAdapter.js` | 543 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `mae: metricMap.MAE?.metric_value \|\| 0,` |
| 图表数值强制补 0 | `frontend\src\utils\dataAdapter.js` | 544 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `rmse: metricMap.RMSE?.metric_value \|\| 0,` |
| 图表数值强制补 0 | `frontend\src\utils\dataAdapter.js` | 545 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `mape: metricMap.MAPE?.metric_value \|\| 0,` |
| 图表数值强制补 0 | `frontend\src\utils\dataAdapter.js` | 546 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `trainWindowSize: metricMap.MAE?.train_window_size \|\| metricMap.RMSE?.train_window_size \|\| 0,` |
| 图表数值强制补 0 | `frontend\src\utils\dataAdapter.js` | 561 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `current.actual_sum += Number(item.actual_demand_count \|\| 0)` |
| 图表数值强制补 0 | `frontend\src\utils\dataAdapter.js` | 562 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `current.predicted_sum += Number(item.predicted_demand_count \|\| 0)` |
| 图表数值强制补 0 | `frontend\src\utils\dataAdapter.js` | 655 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `value: Number(item.lift \|\| 0),` |
| 图表数值强制补 0 | `frontend\src\utils\dataAdapter.js` | 656 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `lineStyle: { width: Math.max(1.5, Number(item.lift \|\| 0) * 1.2) },` |
| 图表数值强制补 0 | `frontend\src\utils\dataAdapter.js` | 693 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `.filter((item) => item.target_major === major && Number(item.matching_score \|\| 0) >= minScore)` |
| 图表数值强制补 0 | `frontend\src\utils\dataAdapter.js` | 694 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `.sort((a, b) => Number(b.matching_score \|\| 0) - Number(a.matching_score \|\| 0))` |
| 图表数值强制补 0 | `frontend\src\utils\dataAdapter.js` | 709 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `const value = Number(score \|\| 0)` |
| 图表数值强制补 0 | `frontend\src\utils\dataAdapter.js` | 723 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `const value = Number(score \|\| 0)` |
| 图表数值强制补 0 | `frontend\src\utils\dataAdapter.js` | 747 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `const scoreOf = (item) => Number(item.similarity_score ?? item.matching_score ?? 0)` |
| 图表数值强制补 0 | `frontend\src\utils\dataAdapter.js` | 761 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `.reduce((sum, item) => sum + Number(item.emp_count \|\| 0), 0)` |
| 图表数值强制补 0 | `frontend\src\utils\dataAdapter.js` | 777 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `value: pick('job_demand_forecast', 'RMSE')?.metric_value \|\| 0,` |
| 图表数值强制补 0 | `frontend\src\utils\dataAdapter.js` | 784 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `value: pick('enrollment_matching', 'Precision@K')?.metric_value \|\| 0,` |
| 图表数值强制补 0 | `frontend\src\utils\dataAdapter.js` | 791 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `value: pick('rule_mining', 'AvgLift')?.metric_value \|\| 0,` |
| 图表数值强制补 0 | `frontend\src\utils\dataAdapter.js` | 798 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `value: pick('job_recommendation', 'AvgTop1Similarity')?.metric_value \|\| 0,` |
| 图表数值强制补 0 | `frontend\src\utils\dataAdapter.js` | 858 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `map[item.school_name] = (map[item.school_name] \|\| 0) + item.emp_count` |
| 图表数值强制补 0 | `frontend\src\utils\dataAdapter.js` | 884 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `current.top_major_map.set(item.major_name, (current.top_major_map.get(item.major_name) \|\| 0) + item.emp_count)` |
| 图表数值强制补 0 | `frontend\src\utils\dataAdapter.js` | 885 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `current.industry_map.set(item.leading_industry_tag, (current.industry_map.get(item.leading_industry_tag) \|\| 0) + item.emp_count)` |
| 图表数值强制补 0 | `frontend\src\utils\dataAdapter.js` | 917 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `map[item.discipline_category] = (map[item.discipline_category] \|\| 0) + item.emp_count` |
| 图表数值强制补 0 | `frontend\src\utils\dataAdapter.js` | 944 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `employment_count: Number(item.employment_count \|\| 0),` |
| 图表数值强制补 0 | `frontend\src\utils\dataAdapter.js` | 945 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `employment_rate_estimate: Number(item.employment_rate_estimate \|\| 0),` |
| 图表数值强制补 0 | `frontend\src\utils\dataAdapter.js` | 946 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `avg_salary: Number(item.avg_salary \|\| 0),` |
| 图表数值强制补 0 | `frontend\src\utils\dataAdapter.js` | 947 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `strategic_ratio: Number(item.strategic_ratio \|\| 0),` |
| 图表数值强制补 0 | `frontend\src\utils\dataAdapter.js` | 948 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `high_skill_ratio: Number(item.high_skill_ratio \|\| 0),` |
| 图表数值强制补 0 | `frontend\src\utils\dataAdapter.js` | 951 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `matched_rule_count: Number(item.matched_rule_count \|\| 0),` |
| 图表数值强制补 0 | `frontend\src\utils\dataAdapter.js` | 952 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `top_rule_support: Number(item.top_rule_support \|\| 0),` |
| 图表数值强制补 0 | `frontend\src\utils\dataAdapter.js` | 953 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `top_rule_confidence: Number(item.top_rule_confidence \|\| 0),` |
| 图表数值强制补 0 | `frontend\src\utils\dataAdapter.js` | 954 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `top_rule_lift: Number(item.top_rule_lift \|\| 0),` |
| 图表数值强制补 0 | `frontend\src\utils\dataAdapter.js` | 955 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `priority_score: Number(item.priority_score \|\| 0),` |
| 图表数值强制补 0 | `frontend\src\utils\dataAdapter.js` | 999 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `avgPriorityScore: Number((rows.reduce((sum, item) => sum + Number(item.priority_score \|\| 0), 0) / total).toFixed(1)),` |
| 图表数值强制补 0 | `frontend\src\utils\dataAdapter.js` | 1000 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `avgEmploymentRate: Number((rows.reduce((sum, item) => sum + Number(item.employment_rate_estimate \|\| 0), 0) / total).toFixed(1)),` |
| 图表数值强制补 0 | `frontend\src\utils\dataAdapter.js` | 1001 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `avgSalary: Number((rows.reduce((sum, item) => sum + Number(item.avg_salary \|\| 0), 0) / total).toFixed(0)),` |
| 图表数值强制补 0 | `frontend\src\utils\dataAdapter.js` | 1097 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `avgTrainingScore: Number((rows.reduce((sum, item) => sum + Number(item.training_quality_score \|\| 0), 0) / total).toFixed(1)),` |
| 图表数值强制补 0 | `frontend\src\utils\dataAdapter.js` | 1098 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `avgEmploymentRate: Number((rows.reduce((sum, item) => sum + Number(item.employment_rate \|\| 0), 0) / total).toFixed(1)),` |
| 图表数值强制补 0 | `frontend\src\utils\dataAdapter.js` | 1099 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `avgIndustryMatch: Number((rows.reduce((sum, item) => sum + Number(item.industry_match_score \|\| 0), 0) / total).toFixed(1)),` |
| 图表数值强制补 0 | `frontend\src\utils\dataAdapter.js` | 1135 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `map[item.leading_industry_tag] = (map[item.leading_industry_tag] \|\| 0) + item.emp_count` |
| 图表数值强制补 0 | `frontend\src\utils\dataAdapter.js` | 1192 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `sample_count: Number(item.total_emp \|\| 0),` |
| 图表数值强制补 0 | `frontend\src\utils\dataAdapter.js` | 1221 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `high: Number(data?.summary?.high \|\| 0),` |
| 图表数值强制补 0 | `frontend\src\utils\dataAdapter.js` | 1222 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `medium: Number(data?.summary?.medium \|\| 0),` |
| 图表数值强制补 0 | `frontend\src\utils\dataAdapter.js` | 1223 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `low: Number(data?.summary?.low \|\| 0),` |
| 图表数值强制补 0 | `frontend\src\utils\dataAdapter.js` | 1236 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `const rankDiff = (severityRank[b.warning_level] \|\| 0) - (severityRank[a.warning_level] \|\| 0)` |
| 图表数值强制补 0 | `frontend\src\utils\dataAdapter.js` | 1266 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `employment_rate: Number(item.employment_rate \|\| 0),` |
| 图表数值强制补 0 | `frontend\src\utils\dataAdapter.js` | 1267 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `avg_salary: Number(item.avg_salary \|\| 0),` |
| 图表数值强制补 0 | `frontend\src\utils\dataAdapter.js` | 1268 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `avg_match_score: Number(item.avg_match_score \|\| 0),` |
| 图表数值强制补 0 | `frontend\src\utils\dataAdapter.js` | 1269 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `forecast_growth_pct: item.forecast_growth_pct == null ? null : Number(item.forecast_growth_pct \|\| 0),` |
| 图表数值强制补 0 | `frontend\src\utils\dataAdapter.js` | 1270 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `rule_lift: Number(item.rule_lift \|\| 0),` |
| 图表数值强制补 0 | `frontend\src\utils\dataAdapter.js` | 1271 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `strategic_ratio: Number(item.strategic_ratio \|\| 0),` |
| 图表数值强制补 0 | `frontend\src\utils\dataAdapter.js` | 1272 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `sample_size: Number(item.sample_size \|\| 0),` |
| 图表数值强制补 0 | `frontend\src\utils\dataAdapter.js` | 1273 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `priority_score: Number(item.priority_score \|\| 0),` |
| 图表数值强制补 0 | `frontend\src\utils\dataAdapter.js` | 1283 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `salary_median: Number(data?.summary?.salary_median \|\| 0),` |
| 图表数值强制补 0 | `frontend\src\utils\dataAdapter.js` | 1284 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `employment_median: Number(data?.summary?.employment_median \|\| 0),` |
| 图表数值强制补 0 | `frontend\src\utils\dataAdapter.js` | 1285 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `match_median: Number(data?.summary?.match_median \|\| 0),` |
| 图表数值强制补 0 | `frontend\src\utils\dataAdapter.js` | 1312 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `(levelRank[b.suggestion_level] \|\| 0) - (levelRank[a.suggestion_level] \|\| 0) \|\|` |
| 图表数值强制补 0 | `frontend\src\components\charts\EmploymentSankey.jsx` | 12 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `value: Number(item?.emp_count \|\| 0),` |
| 图表数值强制补 0 | `frontend\src\components\charts\EmploymentSankey.jsx` | 51 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `return `${params.data.source} → ${params.data.target}<br/>人数：${params.data.value \|\| 0}`` |
| 图表数值强制补 0 | `frontend\src\components\charts\EmploymentSankey.jsx` | 88 | null/undefined 可能被转成 0，导致图表假低值或假全 0。 | 图表缺失值优先保留 null，KPI 再做明确兜底。 | `const hasData = Array.isArray(data) && data.some((item) => Number(item?.emp_count \|\| 0) > 0)` |
| 空图表渲染风险 | `frontend\src\components\charts\EmploymentSankey.jsx` | 98 | 图表可能在空数据时仍渲染坐标轴。 | 渲染前判断 series/data 是否为空，空时显示 Empty。 | `return <ReactECharts option={buildSankeyOption(data)} style={style \|\| { height: '100%', minHeight: 420 }} />` |
